var config = {};

config.port = 3000;

//Authentication
config.auth = false;

//Database
config.database = 'kasir';
config.username = 'root';
config.password = '';
config.table_prefix = '';

//Pagination
config.paginate = true;
config.page_limit = 2;

module.exports = config;